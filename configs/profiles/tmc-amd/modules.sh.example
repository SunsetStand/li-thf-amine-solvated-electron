#!/usr/bin/env bash
# This file is sourced inside every Slurm controller allocation. Put only
# site-wide modules that are compatible with every engine here.
#
# Do not load CP2K, GROMACS, ORCA, or an MPI implementation in this file. Their
# validated TMC mappings live in configs/slurm/tmc-amd-stage-modules.sh because
# each engine requires a different OpenMPI version.
