# Compact hbond_pilot Stage A audit bundle

This directory contains the compact `hbond_pilot` Stage A export. The
report generator intentionally refuses to run until the two ready summaries,
the classical validation summary, the Stage A completion marker, and all six
replica audit directories are present.

Large GROMACS trajectories and restart files do not belong here.
